## 💻 Come Avviare la Simulazione

### 🔧 Prerequisiti

Per eseguire il codice è necessario avere:

- [**Python**](https://www.python.org/) 3.x installato
- Le librerie Python richieste dal progetto

Per installare tutte le librerie necessarie, apri il terminale nella cartella del progetto ed esegui:

```bash
pip install -r requirements.txt
```

### ⚙️ Configurazione

Modifica il file di configurazione `sim_config.py` per adattare i parametri alla simulazione che vuoi eseguire.

Ad esempio, puoi impostare lo scenario, il tempo della simulazione, il numero di iterazioni e altre opzioni specifiche.

### 🚀 Esecuzione

Per avviare la simulazione, apri il terminale nella cartella del progetto ed esegui:

```bash
python run_simulation.py
```
